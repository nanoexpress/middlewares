{
  "name": "@nanoexpress/middlewares",
  "version": "1.0.0",
  "description": "Middleware packages for nanoexpress",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=12.16.0"
  },
  "scripts": {
    "lint": "smartlint",
    "build": "rollup -c",
    "semantic-release": "lerna exec --concurrency 1 -- semantic-release -e semantic-release-monorepo"
  },
  "devDependencies": {
    "@commitlint/cli": "^12.0.1",
    "@commitlint/config-lerna-scopes": "^12.0.1",
    "eslint-config-airlight-node": "^1.1.7",
    "husky": "^4.3.8",
    "husky-config-airlight": "^1.0.2",
    "lerna": "^4.0.0",
    "lint-staged": "^10.5.4",
    "lint-staged-config-airlight": "^1.0.7",
    "prettier-config-airlight": "^1.0.0",
    "rollup": "^2.42.4",
    "smartlint": "^1.1.6",
    "typescript": "4.0.5"
  }
}
